---
phase: "03"
plan: "03"
title: "README.md & Integration Guide Deep Rewrite"
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
  - docs/integration_guide/library-integration-guide.md
  - docs/features/index.md
requirements_addressed: [DOCS-01]
autonomous: true
---

# Plan 03: README.md & Integration Guide Deep Rewrite

<objective>
Deep rewrite of README.md to remove all `docs/modules/` references, update version to v1.2, add Phase 1 security model documentation, close resolved architectural gaps, and update the integration guide with Zod/wrappedFetch contract information.
</objective>

## Tasks

<task id="03-01">
<title>README.md — Remove docs/modules references and fix broken links</title>
<read_first>
- README.md
</read_first>
<action>
1. Update version string: "Version 1.1.0" → "Version 1.2.0"
2. Line 7: Change `docs/modules/index.md` reference to `docs/features/index.md`
3. Line 57: Change "docs/modules" → "docs/features" in philosophy section
4. Line 237: `docs/modules/storage/cache` → `docs/features/storage/governance/cache`
5. Line 553: `docs/modules/ui/` → `docs/features/splash`
6. Line 654: Contribution workflow "docs/modules" → "docs/features"
7. Lines 662-663: New App Integration "docs/modules/storage/" → "docs/features/storage/governance/"
8. Line 669: UI Screen "docs/modules/ui" → "docs/features/splash"
9. Lines 690-693: Quick Nav links — all `docs/modules/` → `docs/features/`
10. Line 700: Remove "Legacy docs (bridge)" row from Quick Navigation table
</action>
<acceptance_criteria>
- `grep -c "docs/modules" README.md` returns 0
- README.md contains "Version 1.2.0"
- All internal links resolve to existing files
</acceptance_criteria>
</task>

<task id="03-02">
<title>README.md — Add v1.2 Security Model enhancements</title>
<read_first>
- README.md (Security Model Summary section, ~line 589)
- .planning/phases/01-baseline-security-ipc-hardening/01-CONTEXT.md
</read_first>
<action>
1. Add three new rows to the Security Model Summary table:
   - `IPC Payload Validation | Zod schema .safeParse() on all handlers | Typed payloads at IPC boundary`
   - `Network Timeout | wrappedFetch with AbortController | All HTTP calls timeout-bounded`
   - `Path Traversal | resolvedPath.startsWith(vaultRoot) | Filesystem ops vault-bounded`
2. Update the "IPC Boundary" row description to mention Zod: "contextBridge preload + Zod schema validation — no direct process.env or unvalidated payloads"
</action>
<acceptance_criteria>
- README.md contains "Zod schema"
- README.md contains "wrappedFetch"
- README.md contains "Path Traversal"
- Security Model Summary table has at least 9 rows
</acceptance_criteria>
</task>

<task id="03-03">
<title>README.md — Update Known Architectural Gaps</title>
<read_first>
- README.md (Known Architectural Gaps section, ~line 602)
- docs/features/audit/v1.2/notification-audit-report.md
- docs/features/audit/v1.2/Onboarding-audit-report.md
</read_first>
<action>
1. Review each gap in the Known Architectural Gaps table.
2. No gaps have been fully closed by Phase 1/2 work — these are structural architectural gaps, not security enforcement gaps. Keep the table as-is.
3. Add a note above the table: "Security enforcement gaps (IPC validation, fetch timeouts, path traversal) were closed in v1.2. The gaps below are structural capabilities not yet implemented."
4. Add a row: `Notification Subsystem | Rate limiting and event schema enforcement were gaps — now resolved in v1.2 via dedicated services | Resolved`
</action>
<acceptance_criteria>
- README.md contains "closed in v1.2"
- README.md contains "Rate limiting and event schema enforcement"
</acceptance_criteria>
</task>

<task id="03-04">
<title>README.md — Update Audit Layer section with v1.2 reports</title>
<read_first>
- README.md (Audit Layer section, ~line 567)
</read_first>
<action>
1. Add a new sub-section after the existing audit table:
   ```markdown
   ### v1.2 Feature Audit Reports
   
   Comprehensive domain-by-domain audit conducted in v1.2. Reports are located in [`docs/features/audit/v1.2/`](docs/features/audit/v1.2/index.md).
   
   | Domain | Match Rate | Key Finding |
   |--------|-----------|-------------|
   | Storage | 100% | Vault segregation and path traversal gating confirmed |
   | Cron | 100% | Job registration and failure throttling confirmed |
   | Splash | 100% | Zod migration for runtime config validation |
   | Communication | 100% | wrappedFetch migration complete |
   | Email | 100% | Pipeline lifecycle and UID idempotency confirmed |
   | Queue/Scheduling | 100% | Multi-lane isolation confirmed |
   | Google Integration | 100% | Mirror constraint enforced |
   | Visual | 90% | Token system complete, Puppeteer rendering deferred |
   | Onboarding | 100% | All UX gaps closed |
   | Notification | 100% | Rate limiting and schema enforcement now covered |
   | Vaidyar | 100% | Most complete domain |
   ```
</action>
<acceptance_criteria>
- README.md contains "v1.2 Feature Audit Reports"
- README.md contains "docs/features/audit/v1.2/index.md"
</acceptance_criteria>
</task>

<task id="03-05">
<title>Update Integration Guide — add Zod and wrappedFetch contract notes</title>
<read_first>
- docs/integration_guide/library-integration-guide.md
</read_first>
<action>
1. Add a "v1.2 Security Contract" section after "Host App Preflight Requirements":
   ```markdown
   ## v1.2 Security Contract
   
   Starting with v1.2, Prana enforces strict security boundaries at the IPC and network layers:
   
   ### IPC Payload Validation (Zod)
   All IPC handlers validate incoming payloads using Zod schemas via `.safeParse()`. Host apps must send correctly typed payloads — unvalidated or malformed payloads will be rejected with structured error responses. Key schemas:
   - `PranaRuntimeConfigSchema` — validates bootstrap config at `app:bootstrap-host`
   - Typed payloads on all `app:*` handlers
   
   ### Network Timeout Enforcement (wrappedFetch)
   All HTTP calls within Prana route through `wrappedFetch` (from `globalFetchWrapper.ts`) which enforces:
   - Default timeout via `AbortController`
   - No raw `fetch()` calls permitted in any service
   - Host apps integrating custom network operations should follow the same pattern
   
   ### Filesystem Path Traversal Prevention
   All filesystem operations are vault-bounded: `resolvedPath.startsWith(vaultRoot)` is enforced by `virtualDriveProvider.ts`. Files outside defined vault directories cannot be accessed.
   ```
2. Add `notificationCentreService.ts`, `notificationRateLimiterService.ts`, `notificationValidationService.ts` to relevant service listings.
</action>
<acceptance_criteria>
- `library-integration-guide.md` contains "v1.2 Security Contract"
- `library-integration-guide.md` contains "Zod"
- `library-integration-guide.md` contains "wrappedFetch"
- `library-integration-guide.md` contains "Path Traversal"
</acceptance_criteria>
</task>

<task id="03-06">
<title>Update docs/features/index.md — add audit v1.2 link and version bump</title>
<read_first>
- docs/features/index.md
</read_first>
<action>
1. Update version: "1.1.0" → "1.2.0"
2. In the Audit Layer section (§Layer 5), add:
   - `* [v1.2 Feature Audit Reports](audit/v1.2/index.md) — **Domain-by-Domain Implementation Verification**`
3. Add a Quick Navigation entry:
   - `| **Review v1.2 Audit Results** | [v1.2 Audit Reports](audit/v1.2/index.md) |`
</action>
<acceptance_criteria>
- `index.md` contains "1.2.0"
- `index.md` contains "audit/v1.2/index.md"
</acceptance_criteria>
</task>

## Verification
- `grep -c "docs/modules" README.md` returns 0
- All new sections are present
- Internal links resolve

## must_haves
- All `docs/modules/` references purged from README.md
- Version updated to v1.2.0
- Security model reflects Phase 1 hardening
- Integration guide documents Zod/wrappedFetch contracts
