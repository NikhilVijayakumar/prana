---
phase: "03"
plan: "02"
title: "Deep Rewrite Feature Specs — Close Resolved Gaps & Add Security Notes"
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/features/notification/notification-centre.md
  - docs/features/Onboarding/onboarding-pipeline-orchestrator.md
  - docs/features/splash/splash-system-initialization.md
  - docs/features/chat/communication.md
  - docs/features/email/email.md
  - docs/features/queue-scheduling/queue-scheduling.md
  - docs/features/Integration/google-ecosystem-integration.md
  - docs/features/vaidyar/vaidyar.md
  - docs/features/visual/visual-identity-engine.md
  - docs/features/cron/cron.md
  - docs/features/storage/virtual-drive.md
requirements_addressed: [DOCS-01]
autonomous: true
---

# Plan 02: Deep Rewrite Feature Specs

<objective>
Update all 11 domain feature specs to close resolved gaps, add Phase 1 security enforcement notes (Zod, wrappedFetch, path traversal), and ensure every spec accurately reflects the current implementation state as documented in the Phase 2 audit reports.
</objective>

## Tasks

<task id="02-01">
<title>Update Notification Centre spec — close Rate Limiting & Event Schema gaps</title>
<read_first>
- docs/features/notification/notification-centre.md
- docs/features/audit/v1.2/notification-audit-report.md
</read_first>
<action>
1. Find the gap table section (§15 or equivalent "Known Gaps" / "Deferred" section).
2. Mark these gaps as **Resolved in v1.2**:
   - "Rate Limiting" → Resolved: `notificationRateLimiterService.ts` now enforces rate limits per spec §8.3
   - "Event Schema Enforcement" → Resolved: `notificationValidationService.ts` provides schema enforcement per spec §13.1
3. Add an "Implementation Services" section (or update existing) listing:
   - `notificationCentreService.ts` — delivery pipeline
   - `notificationStoreService.ts` — persistence
   - `notificationValidationService.ts` — schema enforcement
   - `notificationRateLimiterService.ts` — rate limiting
   - `hookSystemService.ts` — event emission
4. Add a "Security Enforcement (v1.2)" note: "All IPC handlers use typed payloads. Event payloads are validated via `notificationValidationService.ts`. UI rendering sanitizes event payloads per injection protection requirements."
</action>
<acceptance_criteria>
- `notification-centre.md` contains "Resolved in v1.2"
- `notification-centre.md` contains "notificationRateLimiterService"
- `notification-centre.md` contains "notificationValidationService"
- `notification-centre.md` contains "Security Enforcement"
</acceptance_criteria>
</task>

<task id="02-02">
<title>Update Onboarding spec — close UX stage gaps</title>
<read_first>
- docs/features/Onboarding/onboarding-pipeline-orchestrator.md
- docs/features/audit/v1.2/Onboarding-audit-report.md
</read_first>
<action>
1. Find the gap table section.
2. Mark these gaps as **Resolved in v1.2** (per spec §0 runtime update 2026-04-06):
   - "Welcome/Orientation Stage" → Resolved
   - "Policy/Consent Gate" → Resolved
   - "Final Review Checkpoint" → Resolved
   - "Completion Handoff" → Resolved
   - "Resume/Pause Messaging" → Resolved
3. Add "Security Enforcement (v1.2)" note: "IPC handlers `app:onboarding-state` and `app:onboarding-submit` accept typed payloads. Only `onboardingOrchestratorService` controls stage transitions — no manual override paths."
4. Add implementation services listing: `onboardingStageStoreService.ts`, `startupOrchestratorService.ts`.
</action>
<acceptance_criteria>
- `onboarding-pipeline-orchestrator.md` contains "Resolved in v1.2"
- `onboarding-pipeline-orchestrator.md` contains "Security Enforcement"
- `onboarding-pipeline-orchestrator.md` contains "onboardingStageStoreService"
</acceptance_criteria>
</task>

<task id="02-03">
<title>Update Splash spec — document Zod validation migration</title>
<read_first>
- docs/features/splash/splash-system-initialization.md
- docs/features/audit/v1.2/splash-audit-report.md
- src/main/services/pranaRuntimeConfig.ts
</read_first>
<action>
1. Add or update the "Runtime Config Validation" section to document the Zod migration:
   - `PranaRuntimeConfigSchema` defines required fields with `z.string().min(1)` constraints
   - `validatePranaRuntimeConfig()` uses `safeParse()` — invalid config returns `{ valid: false, errors, issues }`
   - Fail-fast: system remains BLOCKED until splash provides valid config
2. Add "Security Enforcement (v1.2)" note: "Bootstrap validation migrated from imperative if-chain to Zod schema. All config fields are strictly validated at the IPC boundary. Invalid payloads trigger structured BLOCKED status reports."
3. Update any references to manual validation logic.
</action>
<acceptance_criteria>
- `splash-system-initialization.md` contains "Zod"
- `splash-system-initialization.md` contains "PranaRuntimeConfigSchema"
- `splash-system-initialization.md` contains "Security Enforcement"
</acceptance_criteria>
</task>

<task id="02-04">
<title>Update Communication spec — add wrappedFetch and security notes</title>
<read_first>
- docs/features/chat/communication.md
- docs/features/audit/v1.2/communication-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)" section:
   - No raw `fetch()` calls — all HTTP operations use `wrappedFetch` with timeout enforcement
   - IPC handlers use typed payloads
   - Channel routing validates sender identity before pipeline entry
2. Ensure the gap table correctly lists WhatsApp bridge and agent loop prevention as still open.
</action>
<acceptance_criteria>
- `communication.md` contains "wrappedFetch"
- `communication.md` contains "Security Enforcement"
</acceptance_criteria>
</task>

<task id="02-05">
<title>Update Email spec — add wrappedFetch and pipeline security notes</title>
<read_first>
- docs/features/email/email.md
- docs/features/audit/v1.2/email-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - IMAP operations use `wrappedFetch` where applicable
   - UID idempotency enforced per account
   - Human-in-the-Loop gate remains for all outbound operations
2. Verify gap table correctly lists attachment handling, backpressure, and PII redaction as open.
</action>
<acceptance_criteria>
- `email.md` contains "Security Enforcement"
- `email.md` contains "wrappedFetch"
</acceptance_criteria>
</task>

<task id="02-06">
<title>Update Queue/Scheduling spec — add execution boundary security notes</title>
<read_first>
- docs/features/queue-scheduling/queue-scheduling.md
- docs/features/audit/v1.2/queue-scheduling-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - Task execution uses `wrappedFetch` for HTTP-bound tasks
   - IPC handlers accept typed payloads for task management
   - Lane isolation prevents concurrency starvation
2. Verify gap table correctly lists adaptive throttling, task DAG, and dead letter queue as open.
</action>
<acceptance_criteria>
- `queue-scheduling.md` contains "Security Enforcement"
</acceptance_criteria>
</task>

<task id="02-07">
<title>Update Google Integration spec — add mirror constraint security notes</title>
<read_first>
- docs/features/Integration/google-ecosystem-integration.md
- docs/features/audit/v1.2/Integration-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - OAuth operations use `wrappedFetch` with timeout enforcement
   - Mirror Constraint enforced: Google data is read-only mirror, no write-back
   - IPC handlers accept typed payloads
2. Verify gap table correctly lists write-back pipeline and conflict resolution as open.
</action>
<acceptance_criteria>
- `google-ecosystem-integration.md` contains "Security Enforcement"
- `google-ecosystem-integration.md` contains "wrappedFetch"
</acceptance_criteria>
</task>

<task id="02-08">
<title>Update Vaidyar spec — add diagnostic completeness notes</title>
<read_first>
- docs/features/vaidyar/vaidyar.md
- docs/features/audit/v1.2/vaidyar-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - `recoveryService.ts` uses `wrappedFetch` for HTTP health probes
   - Vaidyar provides blocking signals (`BLOCKED_SECURITY`, `BLOCKED_STORAGE`) that prevent unsafe runtime startup
   - IPC handlers (`app:get-vaidyar-report`, `app:run-vaidyar-pulse`, `app:run-vaidyar-on-demand`, `app:get-vaidyar-telemetry`) return typed responses
2. Note: Vaidyar is the most complete domain (100% match across all capabilities).
</action>
<acceptance_criteria>
- `vaidyar.md` contains "Security Enforcement"
- `vaidyar.md` contains "wrappedFetch"
</acceptance_criteria>
</task>

<task id="02-09">
<title>Update Visual Identity Engine spec — add token system completeness notes</title>
<read_first>
- docs/features/visual/visual-identity-engine.md
- docs/features/audit/v1.2/visual-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - No raw `fetch()` calls — asset rendering operates locally
   - Template registration includes validation before persistence
   - IPC handlers accept typed payloads
2. Verify gap table correctly lists Puppeteer rendering, Google Docs mapping, live preview, and variable schema enforcement as open.
</action>
<acceptance_criteria>
- `visual-identity-engine.md` contains "Security Enforcement"
</acceptance_criteria>
</task>

<task id="02-10">
<title>Update Cron spec — add scheduling boundary security notes</title>
<read_first>
- docs/features/cron/cron.md
- docs/features/audit/v1.2/cron-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - Job execution boundaries prevent unbounded cron runs
   - Failure throttling with max retry limits enforced
   - IPC handlers accept typed payloads
2. Verify gap table correctly lists persistent job state across reboot as open.
</action>
<acceptance_criteria>
- `cron.md` contains "Security Enforcement"
</acceptance_criteria>
</task>

<task id="02-11">
<title>Update Storage/Virtual Drive spec — add path traversal gating notes</title>
<read_first>
- docs/features/storage/virtual-drive.md
- docs/features/audit/v1.2/storage-audit-report.md
</read_first>
<action>
1. Add "Security Enforcement (v1.2)":
   - Path traversal prevention: `virtualDriveProvider.ts` enforces `resolvedPath.startsWith(vaultRoot)` for all filesystem operations
   - Mount registry isolation ensures segregated vault/system drives
   - IPC handlers accept typed payloads
2. Verify gap table correctly lists vector search RAG indexing and concurrent lock reconciliation as open.
</action>
<acceptance_criteria>
- `virtual-drive.md` contains "Security Enforcement"
- `virtual-drive.md` contains "path traversal" (case-insensitive)
</acceptance_criteria>
</task>

## Verification
- All 11 specs contain "Security Enforcement" section
- Resolved gaps marked with "Resolved in v1.2"
- Open gaps remain in gap tables unchanged

## must_haves
- Every feature spec reflects Phase 1 security hardening
- Resolved gaps are closed in original specs
- Open gaps remain documented
