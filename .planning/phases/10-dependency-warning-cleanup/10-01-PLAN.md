---
phase: 10-dependency-warning-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/ui/mui.d.ts
autonomous: true
requirements:
  - DEP-01
  - DEP-02
must_haves:
  truths:
    - "Dependency-warning cleanup touches only maintainable/direct dependencies per context decisions"
    - "No direct edits are made in node_modules or generated build output directories"
    - "npm install, npm run typecheck, and npm run build pass after reconciliation"
    - "Any remaining warnings are traced and classified as upstream external vs in-repo fixable"
  artifacts:
    - path: package.json
      provides: "Dependency declarations and override policy"
      contains: "dependencies and overrides sections"
    - path: package-lock.json
      provides: "Resolved install graph after dependency reconciliation"
      contains: "resolved transitive package versions"
  key_links:
    - from: "package.json"
      to: "npm install"
      via: "peer dependency resolution"
      pattern: "overrides|sharp|@mui/material|@types/react|react-dom"
    - from: "package-lock.json"
      to: "npm ci"
      via: "clean install verification"
      pattern: "electron-builder|@electron/get|glob|rimraf|boolean|sharp"
    - from: "package.json"
      to: "npm run typecheck"
      via: "TypeScript compilation"
      pattern: "@types/react|@types/react-dom|@mui/material"
---

# Phase 10: Dependency Warning Cleanup - Execution Plan

## Goal
Eliminate npm install peer dependency warnings by reconciling Prana's React, MUI, and type package versions and trimming unnecessary overrides.

Purpose: Remove dependency-resolution noise without reintroducing the Astra compatibility workaround.
Output: A cleaned `package.json`, verified install output, and passing build/typecheck.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dependency-warning-cleanup/10-CONTEXT.md
@package.json
@package-lock.json
@src/ui/mui.d.ts

<objective>
Fix the dependency drift causing npm override warnings while preserving the already-correct Astra export path.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit dependency warning sources</name>
  <files>package.json, package-lock.json</files>
  <action>Trace npm warning chains from manifest to lockfile and classify each as in-repo fixable or upstream external per D-01, D-02, D-05, and D-08. Explicitly enforce D-09 and D-10 by auditing only source/manifest paths and never proposing edits in node_modules or generated build artifacts.</action>
  <verify><automated>npm ls @mui/material @mui/icons-material @emotion/react @emotion/styled @types/react @types/react-dom --depth=0</automated></verify>
  <done>Warning sources are fully mapped and grouped into in-repo fixable vs upstream external, with Astra scope and ownership boundaries preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile versions and trim overrides</name>
  <files>package.json, package-lock.json, src/ui/mui.d.ts</files>
  <action>Apply only direct dependency/override adjustments in package manifests per D-03 and D-04, then refresh lockfile resolution. If dependency alignment changes UI type contracts, update source-owned typings (for example src/ui/mui.d.ts) while still honoring D-09 and D-10 (no node_modules/out edits).</action>
  <verify><automated>npm install</automated></verify>
  <done>Direct dependency pins/overrides are minimal and deterministic, lockfile is refreshed, and fixable warning chains are reduced without violating ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Verify install and build remain green</name>
  <files>package.json, package-lock.json</files>
  <action>Run install/typecheck/build validation per D-07 and perform clean-install verification to satisfy D-08 classification requirements. Document any residual warnings as upstream external only when they cannot be resolved through direct dependency/version strategy in package.json.</action>
  <verify><automated>npm run typecheck && npm run build</automated></verify>
  <done>Typecheck and build pass; residual warnings (if any) are explicitly traced, evidenced, and tagged upstream external vs in-repo fixable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local install tree | External packages and peer metadata can change resolution behavior during install. |
| package.json -> typecheck/build pipeline | Version mismatches can break compilation or bundle generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | package.json / npm install | mitigate | Pin or align React, MUI, and type package versions so peer resolution is deterministic and override warnings are minimized. |
| T-10-02 | D | typecheck/build pipeline | mitigate | Verify the cleaned dependency graph with `npm run typecheck && npm run build` after version reconciliation. |
</threat_model>

<verification>
- `npm install` and clean-install checks are traced to explicit dependency chains.
- `npm run typecheck` succeeds after dependency reconciliation.
- `npm run build` succeeds after dependency reconciliation.
</verification>

<success_criteria>
- `package.json` and `package-lock.json` contain only the direct dependency/override decisions needed for deterministic installs.
- No direct edits were made to `node_modules` or generated build artifacts.
- Remaining install warnings, if any, are evidenced as upstream package metadata/toolchain issues rather than Prana drift.
- The existing Astra export fix remains untouched and in effect.
</success_criteria>

<output>
After completion, create `.planning/phases/10-dependency-warning-cleanup/10-01-SUMMARY.md`
</output>